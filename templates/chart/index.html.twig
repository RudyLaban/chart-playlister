{% extends 'base.html.twig' %}

{% block title %}Hello ChartController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 900px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>{{ title }}</h1>
    <pre>
{#        {{ dump(display_element) }}#}
        <span>
            {% for song in display_element %}
            <span>
                <span>{{ song.song }}</span>
                <span>{{ song.artist }}</span>
            </span>
            {% endfor %}

        </span>
    </pre>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ 'C:/wamp64/www/chart-playlister/src/Controller/ChartController.php'|file_link(0) }}">src/Controller/ChartController.php</a></code></li>
        <li>Your template at <code><a href="{{ 'C:/wamp64/www/chart-playlister/templates/chart/index.html.twig.twig'|file_link(0) }}">templates/chart/index.html.twig</a></code></li>
    </ul>
</div>
{% endblock %}
